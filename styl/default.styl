@import'ress'

body
    width: 100%
    height 100vh
    background: black
    .container
        width 100%
        height 100%
        position: relative
        .time
            position: absolute
            width: 100%
            height: 100%
            display: flex
            justify-content: center
            align-items: center
            #time
                font-size: 8rem
                color: white
